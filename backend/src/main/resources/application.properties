
# Configuration de la base de données H2 (pour le développement)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.h2.console.enabled=true

# Configuration JPA
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=always
spring.jpa.show-sql=true

# Configuration du serveur
server.port=8080

# Configuration CORS - Autoriser le frontend Lovable
cors.allowed.origins=http://localhost:5173,https://lovable.dev,*.lovable.dev
cors.allowed.methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed.headers=*

# JWT Secret (changez ceci en production)
app.jwtSecret=tchatSouvenirSecretKey2024
app.jwtExpirationMs=86400000
jwt.secret=maSuperCleSecrete123
jwt.expiration=86400000

# Minio
minio.url=http://localhost:9001
minio.accessKey=admin
minio.secretKey=admin123456
minio.bucket=tchat-souvenir-books

## Configuration Minio
#minio.url=http://localhost:9000
#minio.access-key=minioadmin
#minio.secret-key=minioadmin
#minio.bucket-name=tchat-souvenir-books

# MinIO configuration
#minio.url=http://localhost:9001
#minio.access-key=admin
#minio.secret-key=admin123456
#minio.bucket-name=media
logging.level.root=DEBUG

spring.main.log-startup-info=true
